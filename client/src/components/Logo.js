import React from 'react';
import logo from '../favicon.ico';

const Logo = () => {
  return (
    <div>
      <img src={logo} alt="jobify" className="logo" />
    </div>
  );
};

export default Logo;
